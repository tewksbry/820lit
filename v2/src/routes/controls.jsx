import React, { Component } from "react";
import { ControlsContainer } from "../containers";

export default class ControlsRoute extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <ControlsContainer />;
  }
}
