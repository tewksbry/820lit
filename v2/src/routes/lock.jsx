import React, { Component } from "react";
import { Lock } from "../components";

export default class LockRoute extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <Lock />;
  }
}
